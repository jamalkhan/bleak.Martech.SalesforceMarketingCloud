<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SfmcApp.SfmcInstanceMenuPage"
             Title="SfmcInstanceMenuPage">
    <ScrollView>
        <VerticalStackLayout>
            <Label 
                Text="{Binding Name, StringFormat='Connected to SFMC \ {0}'}"
                VerticalOptions="Center" 
                HorizontalOptions="Center" />
            <Button
                x:Name="ShowDataExtensionsBtn"
                Text="Show Data Extensions" 
                SemanticProperties.Hint="Show Data Extensions"
                Clicked="OnShowDataExtensionsClicked"
                HorizontalOptions="Fill" />
            <Button
                x:Name="ShowAssetsBtn"
                Text="Show Assets" 
                SemanticProperties.Hint="Show Assets"
                Clicked="OnShowAssetClicked"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>